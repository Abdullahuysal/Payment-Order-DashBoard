VITE_API_BASE_URL=http://localhost:5080
VITE_DEFAULT_ENV=dev
VITE_HTTP_TIMEOUT_MS=10000

# Test Koşumları mock katmanı: false → gerçek apiClient()
VITE_TESTRUNS_MOCK=true

# Sipariş Kontrol mock katmanı: false → gerçek apiClient()
VITE_ORDERS_MOCK=true

# Geliştirici Araçları mock katmanı: false → gerçek apiClient()
VITE_DEVTOOLS_MOCK=true
